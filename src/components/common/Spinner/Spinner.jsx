import { StyleSpinner } from '../Spinner/Spinner.style'

const Spinner = () => {
    return (
        <StyleSpinner>
            <div className="spinnerContainer">
                <div className="spinner"></div>
            </div>
        </StyleSpinner>
    );
};

export default Spinner